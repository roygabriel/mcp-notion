name: Build and Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g., v1.0.0)'
        required: true
        default: 'v1.0.0'

permissions:
  contents: write

jobs:
  build:
    name: Build Binaries
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - os: linux
            arch: amd64
            output: mcp-notion-linux-amd64
          - os: linux
            arch: arm64
            output: mcp-notion-linux-arm64
          - os: darwin
            arch: amd64
            output: mcp-notion-macos-amd64
          - os: darwin
            arch: arm64
            output: mcp-notion-macos-arm64
          - os: windows
            arch: amd64
            output: mcp-notion-windows-amd64.exe
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Get dependencies
        run: go mod download

      - name: Build binary
        env:
          GOOS: ${{ matrix.os }}
          GOARCH: ${{ matrix.arch }}
          CGO_ENABLED: 0
        run: |
          VERSION="${GITHUB_REF#refs/tags/}"
          go build -ldflags="-s -w -X main.version=${VERSION}" -o ${{ matrix.output }} .
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.output }}
          path: ${{ matrix.output }}
          retention-days: 1

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./binaries

      - name: Display structure of downloaded files
        run: ls -R ./binaries

      - name: Create checksums
        run: |
          cd binaries
          for dir in */; do
            cd "$dir"
            for file in *; do
              sha256sum "$file" > "${file}.sha256"
            done
            cd ..
          done
          cd ..

      - name: Get version
        id: get_version
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          fi

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.get_version.outputs.version }}
          name: Release ${{ steps.get_version.outputs.version }}
          draft: false
          prerelease: false
          generate_release_notes: true
          files: |
            binaries/**/*
          body: |
            ## Notion MCP Server ${{ steps.get_version.outputs.version }}
            
            Model Context Protocol server for Notion integration using the official REST API.
            
            ### Features
            - ✅ Search across pages and databases
            - ✅ Create, read, update, and delete pages
            - ✅ Query databases with complex filters
            - ✅ Manage blocks and content
            - ✅ Work with comments and users
            - ✅ Simplified helper tools with markdown support
            - ✅ Cross-platform support (Linux, macOS, Windows)
            - ✅ Rate limiting and pagination handling
            
            ### Installation
            
            1. Download the binary for your platform below
            2. Make it executable (Linux/macOS): `chmod +x mcp-notion-*`
            3. Create a Notion Integration at https://www.notion.so/my-integrations
            4. Set `NOTION_API_TOKEN` environment variable
            5. Share pages/databases with your integration
            6. Run the server or add to Claude Desktop
            
            ### Platform Downloads
            
            - **Linux (x64)**: `mcp-notion-linux-amd64`
            - **Linux (ARM64)**: `mcp-notion-linux-arm64`
            - **macOS (Intel)**: `mcp-notion-macos-amd64`
            - **macOS (Apple Silicon)**: `mcp-notion-macos-arm64`
            - **Windows (x64)**: `mcp-notion-windows-amd64.exe`
            
            ### Checksums
            
            SHA256 checksums are provided for each binary (`.sha256` files).
            
            ### Documentation
            
            See the [README](https://github.com/rgabriel/mcp-notion/blob/main/README.md) for:
            - Notion integration setup instructions
            - How to share pages/databases with the integration
            - Claude Desktop configuration
            - All 24 available tools with examples
            - Filter and sort syntax
            - Block and property type references
            - Troubleshooting guide
            
            ### Requirements
            
            - Notion account
            - Notion Integration Token from notion.so/my-integrations
            - Pages/databases must be explicitly shared with the integration
            
            ### Quick Start
            
            ```bash
            # Set your integration token
            export NOTION_API_TOKEN="secret_your_token_here"
            
            # Run the server
            ./mcp-notion-<platform>
            ```
            
            For Claude Desktop integration, see the README for platform-specific configuration.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
